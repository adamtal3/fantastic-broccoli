export default class Step2 extends React.Component {
  render() {
    return (
      <div>
        <h3>Step 2 - React element</h3>
        
        <span className="information">Here you should add a controlled text input with a clear button - changing the text in the input should change the <strong>state</strong>, pressing the button should clear the state text which will clear the input.:</span><br/>
        [TEXT INPUT] [CLEAR BUTTON]
      </div>);
  }
}